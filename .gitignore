# Dependencies
node_modules

# Build output
dist
dist-ssr
*.tsbuildinfo

# Environment & secrets
.env
.env.local
.env.*.local
vault-keypair.json
.devnet-config.json

# Solana / Anchor build
programs/target/
target/

# Supabase local
supabase/.temp/

# IDE & editor
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# AI / agent tool config (may contain secrets)
.claude/
.mcp.json
.mcp-deploy-open-args.json
.open-position-mcp-args.json
open-position-mcp-deploy.json
skills-lock.json

# Logs
logs
*.log
npm-debug.log*
pnpm-debug.log*
lerna-debug.log*

# Lockfiles (keep pnpm-lock.yaml, exclude others)
package-lock.json
yarn.lock
bun.lock
bun.lockb

# Yarn (legacy - no longer used)
.pnp.*
.yarn/*

# OS
Thumbs.db

# Don't commit build artifacts or keypairs
target/deploy/*.so
target/deploy/*-keypair.json
target/idl/
target/types/
.superstack/

# Turbo
.turbo
